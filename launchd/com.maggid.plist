<?xml version="1.0" encoding="UTF-8"?>
<!--
  LaunchAgent for the shared maggid daemon.

  Install (substitutes the absolute path, which launchd requires):

    sed "s|__VENV__|$PWD/.venv|g" launchd/com.maggid.plist \
      > ~/Library/LaunchAgents/com.maggid.plist
    launchctl load ~/Library/LaunchAgents/com.maggid.plist

  This must be a LaunchAgent, not a LaunchDaemon. A LaunchDaemon runs outside
  the user's GUI session and cannot reach the audio device.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.maggid</string>

    <key>ProgramArguments</key>
    <array>
        <string>__VENV__/bin/maggid</string>
        <string>--transport</string>
        <string>http</string>
        <string>--host</string>
        <string>127.0.0.1</string>
        <string>--port</string>
        <string>8765</string>
    </array>

    <key>RunAtLoad</key>
    <true/>

    <!-- Restart if the daemon dies. One process now serves every session, so a
         crash silences the whole fleet until it comes back. Claude Code
         reconnects on its next tool call; only the utterance in flight is lost. -->
    <key>KeepAlive</key>
    <true/>

    <!-- Don't respawn faster than this if it crashes on startup. -->
    <key>ThrottleInterval</key>
    <integer>10</integer>

    <key>StandardOutPath</key>
    <string>/tmp/maggid.log</string>
    <key>StandardErrorPath</key>
    <string>/tmp/maggid.err</string>

    <key>ProcessType</key>
    <string>Interactive</string>
</dict>
</plist>
